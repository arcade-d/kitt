# Écran companion CRT bi-chrome K2000 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer l'écran companion (Flame) en moniteur CRT bi-chrome K2000 : cadre cathodique, Larson rouge segmenté en bandeau, voicebox ambre (oscilloscope) et transcript téléscripteur, piloté par l'état du pipeline.

**Architecture:** On réécrit `lib/ui/companion_game.dart` en composants Flame focalisés (CRT, scanner, voicebox, transcript) assemblés par `KittGame`. Une fonction pure `visual_mapping.dart` traduit l'état → paramètres visuels (testable). Le pipeline expose un nouveau signal **`userHeard`** (utterance reconnue) ; `partialResponse` existe déjà pour la réponse. L'écran ponte ces flux vers des `ValueNotifier` consommés par le jeu. Couleurs/typo via `KittColors`/`KittText` (déjà dans `lib/ui/theme/kitt_theme.dart`).

**Tech Stack:** Flutter/Dart, Flame (`PositionComponent`, `CustomPaint`/`Canvas`, `TextPaint`), Riverpod. Pas de Flutter en local → vérif via CI (`analyze`/`test`/build) + rendu device.

**Hors périmètre (plans de suivi) :** séquence de boot « K.I.T.T. SYSTEMS ONLINE » ; voicebox `responding` pilotée par l'enveloppe TTS réelle (v1 = synthétique).

Spec : `docs/superpowers/specs/2026-06-13-ecran-crt-k2000-design.md`

---

## Structure des fichiers

- **Créer** `lib/ui/visual_mapping.dart` — fonctions pures état → vitesse scanner / mode voicebox. Testable.
- **Créer** `test/ui/visual_mapping_test.dart`.
- **Modifier** `lib/application/companion_pipeline.dart` — ajouter le flux `userHeard` (utterance reconnue), émis dans `runTurn`.
- **Modifier** `test/application/companion_pipeline_test.dart` — couvrir `userHeard`.
- **Modifier** `lib/application/providers.dart` — `userHeardProvider` + `responseTokenProvider`.
- **Réécrire** `lib/ui/companion_game.dart` — `KittGame` + `CrtComponent` + `LarsonScannerComponent` + `VoiceboxComponent` + `TranscriptComponent`.
- **Modifier** `lib/ui/companion_screen.dart` — ponter les nouveaux flux vers des `ValueNotifier` et les passer au jeu.

---

## Task 1 : Mapping visuel pur (état → paramètres)

**Files:**
- Create: `lib/ui/visual_mapping.dart`
- Test: `test/ui/visual_mapping_test.dart`

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `test/ui/visual_mapping_test.dart` :

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:kitt/application/pipeline_state.dart';
import 'package:kitt/ui/visual_mapping.dart';

void main() {
  group('scannerSpeed', () {
    test('veille lente, réflexion la plus rapide', () {
      expect(scannerSpeed(PipelineState.idle), lessThan(1.0));
      expect(
        scannerSpeed(PipelineState.thinking),
        greaterThan(scannerSpeed(PipelineState.listening)),
      );
      expect(
        scannerSpeed(PipelineState.listening),
        greaterThan(scannerSpeed(PipelineState.idle)),
      );
    });
  });

  group('voiceboxMode', () {
    test('mappe chaque état vers un mode', () {
      expect(voiceboxMode(PipelineState.idle), VoiceboxMode.flat);
      expect(voiceboxMode(PipelineState.listening), VoiceboxMode.mic);
      expect(voiceboxMode(PipelineState.thinking), VoiceboxMode.noise);
      expect(voiceboxMode(PipelineState.responding), VoiceboxMode.speaking);
      expect(voiceboxMode(PipelineState.clarifying), VoiceboxMode.pulse);
    });
  });
}
```

- [ ] **Step 2 : Lancer le test → échec**

Run: `flutter test test/ui/visual_mapping_test.dart`
Expected: ÉCHEC compilation (`visual_mapping.dart` absent).
*(Local sans Flutter : non exécutable → validé en CI. Lancer au moins `dart format`.)*

- [ ] **Step 3 : Implémenter**

Créer `lib/ui/visual_mapping.dart` :

```dart
import '../application/pipeline_state.dart';

/// Mode d'animation de la voicebox (oscilloscope) selon l'état.
enum VoiceboxMode { flat, mic, noise, speaking, pulse }

/// Vitesse du balayage Larson selon l'état (unités arbitraires, cohérentes
/// avec l'ancien comportement : veille lente, réflexion rapide).
double scannerSpeed(PipelineState state) {
  switch (state) {
    case PipelineState.idle:
      return 0.6;
    case PipelineState.listening:
    case PipelineState.clarifying:
      return 2.4;
    case PipelineState.thinking:
      return 3.6;
    case PipelineState.responding:
      return 1.8;
  }
}

/// Mode de la voicebox selon l'état.
VoiceboxMode voiceboxMode(PipelineState state) {
  switch (state) {
    case PipelineState.idle:
      return VoiceboxMode.flat;
    case PipelineState.listening:
      return VoiceboxMode.mic;
    case PipelineState.thinking:
      return VoiceboxMode.noise;
    case PipelineState.responding:
      return VoiceboxMode.speaking;
    case PipelineState.clarifying:
      return VoiceboxMode.pulse;
  }
}
```

- [ ] **Step 4 : Lancer le test → succès** (CI)

Run: `flutter test test/ui/visual_mapping_test.dart`
Expected: PASS.

- [ ] **Step 5 : Format**

Run: `dart format lib/ui/visual_mapping.dart test/ui/visual_mapping_test.dart`
Expected: éventuel reformat appliqué ; relancer `dart format --output=none --set-exit-if-changed` sur ces 2 fichiers → 0 changed.

- [ ] **Step 6 : Commit**

```bash
git add lib/ui/visual_mapping.dart test/ui/visual_mapping_test.dart
git commit -m "feat(ui): mapping pur état -> vitesse scanner / mode voicebox"
```

---

## Task 2 : Signal `userHeard` dans le pipeline

**Files:**
- Modify: `lib/application/companion_pipeline.dart`
- Test: `test/application/companion_pipeline_test.dart`

- [ ] **Step 1 : Écrire le test qui échoue**

Ajouter dans `test/application/companion_pipeline_test.dart` (dans `main()`), un test qui vérifie que l'utterance reconnue est émise. Il s'appuie sur les mocks déjà utilisés par les autres tests du fichier (réutiliser les doubles existants ; si le fichier expose un helper de construction de pipeline, l'utiliser). Code du test :

```dart
  test('userHeard émet l’utterance reconnue lors d’un tour', () async {
    final pipeline = makeTestPipeline(); // helper existant du fichier
    await pipeline.initialize();
    final heard = <String>[];
    final sub = pipeline.userHeard.listen(heard.add);

    await pipeline.runTurn(<double>[0.1, 0.2, 0.3], 16000);

    await sub.cancel();
    expect(heard, isNotEmpty);
    await pipeline.dispose();
  });
```

> Si `makeTestPipeline()` n'existe pas dans le fichier, construire le pipeline comme les autres tests du même fichier (mêmes mocks `MockStt/MockLlm/MockTts/...`). Le `MockStt` doit renvoyer une utterance exploitable pour atteindre l'émission (vérifier le double existant ; sinon utiliser celui qui produit un texte non vide).

- [ ] **Step 2 : Lancer le test → échec** (CI)

Run: `flutter test test/application/companion_pipeline_test.dart`
Expected: ÉCHEC (`userHeard` non défini).

- [ ] **Step 3 : Implémenter le flux**

Dans `lib/application/companion_pipeline.dart` :

a) Ajouter le contrôleur à côté de `_partial` :

```dart
  final StreamController<String> _userHeard =
      StreamController<String>.broadcast();
```

b) Exposer le flux à côté de `partialResponse` :

```dart
  /// Flux de l'utterance reconnue (pour le transcript de l'UI).
  Stream<String> get userHeard => _userHeard.stream;
```

c) Dans `runTurn`, **après** `conversation.addUser(heard.text, ...)` (donc une fois l'utterance jugée exploitable) :

```dart
      conversation.addUser(heard.text, sttConfidence: heard.confidence);
      _userHeard.add(heard.text);
```

d) Dans `dispose()`, fermer le contrôleur :

```dart
  Future<void> dispose() async {
    await _states.close();
    await _partial.close();
    await _userHeard.close();
  }
```

- [ ] **Step 4 : Lancer le test → succès** (CI)

Run: `flutter test test/application/companion_pipeline_test.dart`
Expected: PASS (et tests existants toujours verts).

- [ ] **Step 5 : Format**

Run: `dart format --output=none --set-exit-if-changed lib/application/companion_pipeline.dart test/application/companion_pipeline_test.dart`
Expected: 0 changed (sinon `dart format` puis recommit).

- [ ] **Step 6 : Commit**

```bash
git add lib/application/companion_pipeline.dart test/application/companion_pipeline_test.dart
git commit -m "feat(app): exposer userHeard (utterance reconnue) pour le transcript"
```

---

## Task 3 : Providers pour transcript

**Files:**
- Modify: `lib/application/providers.dart`

- [ ] **Step 1 : Ajouter les providers**

Dans `lib/application/providers.dart`, après `audioLevelProvider` :

```dart
/// Utterance reconnue (pour le transcript). Émet à chaque tour exploitable.
final userHeardProvider = StreamProvider<String>((ref) async* {
  final pipeline = await ref.watch(pipelineProvider.future);
  yield* pipeline.userHeard;
});

/// Tokens de la réponse de KITT en flux (téléscripteur).
final responseTokenProvider = StreamProvider<String>((ref) async* {
  final pipeline = await ref.watch(pipelineProvider.future);
  yield* pipeline.partialResponse;
});
```

- [ ] **Step 2 : Format + analyze** (CI pour analyze)

Run: `dart format --output=none --set-exit-if-changed lib/application/providers.dart`
Expected: 0 changed.

- [ ] **Step 3 : Commit**

```bash
git add lib/application/providers.dart
git commit -m "feat(app): providers userHeard + responseToken (transcript)"
```

---

## Task 4 : Composants Flame CRT (réécriture de `companion_game.dart`)

**Files:**
- Modify (réécriture complète): `lib/ui/companion_game.dart`

> Note rendu : on calque les patterns Flame **déjà en place** dans ce fichier (`PositionComponent` + `HasGameReference<KittGame>`, `render(Canvas)`, `game.size.toSize()`, `Paint`, `RadialGradient`, `MaskFilter`, `TextPaint`). Couleurs/typo via `KittColors`/`KittText`. Voicebox `responding` = synthétique (v1).

- [ ] **Step 1 : Réécrire le fichier**

Remplacer **tout** `lib/ui/companion_game.dart` par :

```dart
import 'dart:math' as math;

import 'package:flame/components.dart';
import 'package:flame/game.dart';
import 'package:flutter/foundation.dart';
import 'package:flutter/widgets.dart';

import '../application/pipeline_state.dart';
import 'theme/kitt_theme.dart';
import 'visual_mapping.dart';

/// Jeu Flame du companion : moniteur CRT bi-chrome K2000 (cf. spec écran CRT).
/// Empile : cadre CRT, voicebox (oscilloscope ambre), Larson rouge segmenté
/// (bandeau), transcript téléscripteur, puis l'overlay CRT (scanlines+vignette).
class KittGame extends FlameGame {
  KittGame({
    required this.stateListenable,
    required this.levelListenable,
    required this.userTextListenable,
    required this.responseTextListenable,
  });

  final ValueListenable<PipelineState> stateListenable;
  final ValueListenable<double> levelListenable;
  final ValueListenable<String> userTextListenable;
  final ValueListenable<String> responseTextListenable;

  @override
  Color backgroundColor() => KittColors.black;

  @override
  Future<void> onLoad() async {
    await addAll(<Component>[
      CrtBackgroundComponent(),
      LarsonScannerComponent(stateListenable: stateListenable),
      VoiceboxComponent(
        stateListenable: stateListenable,
        levelListenable: levelListenable,
      ),
      TranscriptComponent(
        stateListenable: stateListenable,
        userTextListenable: userTextListenable,
        responseTextListenable: responseTextListenable,
      ),
      CrtOverlayComponent(),
    ]);
  }
}

/// Fond de l'écran cathodique : lueur radiale ambre + grille phosphore.
class CrtBackgroundComponent extends PositionComponent
    with HasGameReference<KittGame> {
  @override
  void render(Canvas canvas) {
    final Size s = game.size.toSize();
    final Rect full = Offset.zero & s;

    // Lueur radiale ambre centrale.
    final Rect glow = Rect.fromCenter(
      center: full.center,
      width: s.width * 1.1,
      height: s.height * 0.9,
    );
    canvas.drawRect(
      full,
      Paint()
        ..shader = RadialGradient(
          colors: <Color>[
            KittColors.amber.withValues(alpha: 0.06),
            const Color(0xFF000000),
          ],
          stops: const <double>[0.0, 0.75],
        ).createShader(glow),
    );

    // Grille phosphore (lignes faibles).
    final Paint grid = Paint()
      ..color = KittColors.amber.withValues(alpha: 0.06)
      ..strokeWidth = 1;
    const double step = 22;
    for (double x = 0; x <= s.width; x += step) {
      canvas.drawLine(Offset(x, 0), Offset(x, s.height), grid);
    }
    for (double y = 0; y <= s.height; y += step) {
      canvas.drawLine(Offset(0, y), Offset(s.width, y), grid);
    }
  }
}

/// Le balayage Larson : bandeau de cellules rouges avec traînée qui s'estompe.
class LarsonScannerComponent extends PositionComponent
    with HasGameReference<KittGame> {
  LarsonScannerComponent({required this.stateListenable});

  final ValueListenable<PipelineState> stateListenable;

  static const int _cells = 8;
  double _t = 0;

  @override
  void update(double dt) {
    _t += dt * scannerSpeed(stateListenable.value);
  }

  @override
  void render(Canvas canvas) {
    final Size s = game.size.toSize();
    final double y = s.height * 0.12;
    final double margin = s.width * 0.08;
    final double span = s.width - margin * 2;
    final double cellW = span / _cells;

    // Position continue 0..(_cells-1) en ping-pong.
    final double pos = (0.5 + 0.5 * math.sin(_t)) * (_cells - 1);

    for (int i = 0; i < _cells; i++) {
      final double dist = (i - pos).abs();
      final double intensity = (1.0 - dist / 2.0).clamp(0.0, 1.0);
      final Rect cell = Rect.fromLTWH(
        margin + i * cellW + cellW * 0.12,
        y - 4,
        cellW * 0.76,
        8,
      );
      final Paint p = Paint()
        ..color = Color.lerp(
          KittColors.scarletDim.withValues(alpha: 0.25),
          KittColors.scarlet,
          intensity,
        )!;
      if (intensity > 0.05) {
        p.maskFilter = MaskFilter.blur(BlurStyle.normal, 6 * intensity);
      }
      canvas.drawRRect(
        RRect.fromRectAndRadius(cell, const Radius.circular(2)),
        p,
      );
    }
  }
}

/// La voicebox : oscilloscope ambre. Amplitude selon le mode (cf. visual_mapping).
class VoiceboxComponent extends PositionComponent
    with HasGameReference<KittGame> {
  VoiceboxComponent({
    required this.stateListenable,
    required this.levelListenable,
  });

  final ValueListenable<PipelineState> stateListenable;
  final ValueListenable<double> levelListenable;

  static const int _points = 64;
  double _phase = 0;

  @override
  void update(double dt) {
    _phase += dt * 6;
  }

  double _amplitudeFor(VoiceboxMode mode) {
    switch (mode) {
      case VoiceboxMode.flat:
        return 0.04;
      case VoiceboxMode.mic:
        return 0.1 + levelListenable.value.clamp(0.0, 1.0) * 0.8;
      case VoiceboxMode.noise:
        return 0.5;
      case VoiceboxMode.speaking:
        return 0.6;
      case VoiceboxMode.pulse:
        return 0.3 + 0.3 * (0.5 + 0.5 * math.sin(_phase * 1.5));
    }
  }

  @override
  void render(Canvas canvas) {
    final Size s = game.size.toSize();
    final double midY = s.height * 0.5;
    final double margin = s.width * 0.08;
    final double span = s.width - margin * 2;
    final double maxAmp = s.height * 0.16;

    final VoiceboxMode mode = voiceboxMode(stateListenable.value);
    final double amp = _amplitudeFor(mode) * maxAmp;

    final Path path = Path();
    for (int i = 0; i <= _points; i++) {
      final double fx = i / _points;
      final double x = margin + fx * span;
      // Forme d'onde : somme de sinus modulée, enveloppe en cloche aux bords.
      final double env = math.sin(fx * math.pi);
      final double wave =
          math.sin(fx * math.pi * 6 + _phase) * 0.6 +
          math.sin(fx * math.pi * 13 - _phase * 1.3) * 0.4;
      final double y = midY + wave * amp * env;
      if (i == 0) {
        path.moveTo(x, y);
      } else {
        path.lineTo(x, y);
      }
    }

    canvas.drawPath(
      path,
      Paint()
        ..style = PaintingStyle.stroke
        ..strokeWidth = 2
        ..color = KittColors.amber
        ..maskFilter = const MaskFilter.blur(BlurStyle.normal, 2),
    );

    // Libellé d'état (afficheur ambre) sous l'oscilloscope.
    TextPaint(style: KittText.label).render(
      canvas,
      _stateLabel(stateListenable.value),
      Vector2(margin, s.height * 0.66),
    );
  }

  String _stateLabel(PipelineState state) {
    switch (state) {
      case PipelineState.idle:
        return 'EN VEILLE';
      case PipelineState.listening:
        return 'À L\'ÉCOUTE';
      case PipelineState.thinking:
        return 'RÉFLEXION';
      case PipelineState.responding:
        return 'KITT RÉPOND';
      case PipelineState.clarifying:
        return 'PARDON ?';
    }
  }
}

/// Transcript téléscripteur : utterance reconnue + réponse de KITT (ambre).
class TranscriptComponent extends PositionComponent
    with HasGameReference<KittGame> {
  TranscriptComponent({
    required this.stateListenable,
    required this.userTextListenable,
    required this.responseTextListenable,
  });

  final ValueListenable<PipelineState> stateListenable;
  final ValueListenable<String> userTextListenable;
  final ValueListenable<String> responseTextListenable;

  @override
  void render(Canvas canvas) {
    final Size s = game.size.toSize();
    final double margin = s.width * 0.08;
    final double maxWidth = s.width - margin * 2;
    double y = s.height * 0.72;

    final String user = userTextListenable.value;
    if (user.isNotEmpty) {
      y = _line(canvas, '> $user', KittColors.steel, margin, y, maxWidth);
    }
    final String resp = responseTextListenable.value;
    if (resp.isNotEmpty) {
      _line(canvas, 'KITT: $resp', KittColors.amber, margin, y, maxWidth);
    }
  }

  /// Rend une ligne (avec wrap simple) ; renvoie le y suivant.
  double _line(
    Canvas canvas,
    String text,
    Color color,
    double x,
    double y,
    double maxWidth,
  ) {
    final TextPaint tp = TextPaint(
      style: KittText.mono.copyWith(color: color),
    );
    for (final line in _wrap(text, tp, maxWidth)) {
      tp.render(canvas, line, Vector2(x, y));
      y += 16;
    }
    return y + 4;
  }

  List<String> _wrap(String text, TextPaint tp, double maxWidth) {
    final words = text.split(' ');
    final out = <String>[];
    var current = '';
    for (final w in words) {
      final attempt = current.isEmpty ? w : '$current $w';
      if (tp.getLineMetrics(attempt).width > maxWidth && current.isNotEmpty) {
        out.add(current);
        current = w;
      } else {
        current = attempt;
      }
    }
    if (current.isNotEmpty) out.add(current);
    return out.take(4).toList(growable: false);
  }
}

/// Overlay CRT : scanlines + vignette, rendu par-dessus tout.
class CrtOverlayComponent extends PositionComponent
    with HasGameReference<KittGame> {
  @override
  void render(Canvas canvas) {
    final Size s = game.size.toSize();
    final Rect full = Offset.zero & s;

    // Scanlines horizontales.
    final Paint scan = Paint()..color = const Color(0x10000000);
    for (double y = 0; y < s.height; y += 3) {
      canvas.drawRect(Rect.fromLTWH(0, y, s.width, 1), scan);
    }

    // Vignette (assombrissement des bords).
    canvas.drawRect(
      full,
      Paint()
        ..shader = RadialGradient(
          colors: const <Color>[Color(0x00000000), Color(0xCC000000)],
          stops: const <double>[0.65, 1.0],
        ).createShader(full),
    );
  }
}
```

- [ ] **Step 2 : Format + analyze (CI)**

Run: `dart format --output=none --set-exit-if-changed lib/ui/companion_game.dart`
Expected: 0 changed. L'analyze (CI) doit être propre : pas de `TextPaint.getLineMetrics` inexistant — vérifier l'API Flame (`TextPaint.getLineMetrics(String)` renvoie `LineMetrics` avec `.width`). Si indisponible dans la version de Flame, remplacer `_wrap` par une coupe par nombre de caractères (≈ `maxWidth ~/ 8`).

- [ ] **Step 3 : Commit**

```bash
git add lib/ui/companion_game.dart
git commit -m "feat(ui): écran companion en moniteur CRT bi-chrome K2000"
```

---

## Task 5 : Câblage de l'écran (`companion_screen.dart`)

**Files:**
- Modify: `lib/ui/companion_screen.dart`

- [ ] **Step 1 : Ajouter les notifiers transcript et les ponter**

Dans `_CompanionScreenState` de `lib/ui/companion_screen.dart` :

a) Ajouter les champs à côté de `_state` / `_level` :

```dart
  final ValueNotifier<String> _userText = ValueNotifier<String>('');
  final ValueNotifier<String> _responseText = ValueNotifier<String>('');
```

b) Mettre à jour la construction du jeu :

```dart
  late final KittGame _game = KittGame(
    stateListenable: _state,
    levelListenable: _level,
    userTextListenable: _userText,
    responseTextListenable: _responseText,
  );
```

c) Les libérer dans `dispose()` :

```dart
    _userText.dispose();
    _responseText.dispose();
```

d) Dans `build`, après les `ref.listen` existants, ajouter :

```dart
    ref.listen<AsyncValue<String>>(userHeardProvider, (_, next) {
      next.whenData((t) {
        _userText.value = t;
        _responseText.value = '';
      });
    });
    ref.listen<AsyncValue<String>>(responseTokenProvider, (_, next) {
      next.whenData((tok) => _responseText.value += tok);
    });
```

- [ ] **Step 2 : Format + analyze (CI)**

Run: `dart format --output=none --set-exit-if-changed lib/ui/companion_screen.dart`
Expected: 0 changed.

- [ ] **Step 3 : Commit**

```bash
git add lib/ui/companion_screen.dart
git commit -m "feat(ui): ponter userHeard/response vers le transcript CRT"
```

---

## Task 6 : Vérification finale + push CI

**Files:** aucun (validation)

- [ ] **Step 1 : Gates locales possibles**

Run: `dart format --output=none --set-exit-if-changed .`
Expected: format OK (le reste — `analyze`/`test`/build — via CI).

- [ ] **Step 2 : Pousser la branche et ouvrir la PR**

```bash
git push -u origin feat/ecran-crt-k2000
gh pr create --base main --title "feat(ui): écran companion CRT bi-chrome K2000" --body "Refonte de l'écran companion en moniteur CRT K2000 (cadre, Larson segmenté, voicebox ambre, transcript). Signal userHeard ajouté. Voicebox responding synthétique (v1). Boot sequence = suivi. Specs: docs/superpowers/specs/2026-06-13-ecran-crt-k2000-design.md"
```

- [ ] **Step 3 : Surveiller la CI** jusqu'au vert (`analyze`/`test`/build). Corriger au besoin (cf. risques API Flame ci-dessous), puis valider le rendu sur device.

---

## Risques / points de vigilance (CI sans Flutter local)

- **API Flame `TextPaint`** : `TextPaint(style: TextStyle).render(canvas, text, Vector2)` et `getLineMetrics(String).width` — confirmer sur la version de Flame du repo. Repli `_wrap` par nb de caractères si `getLineMetrics` absent.
- **`Color.lerp(...)!`** (non-null) : les deux couleurs sont non-nulles → sûr.
- **`withValues(alpha:)`** : déjà utilisé ailleurs dans le repo (API récente) → OK.
- **`require_trailing_commas`** : garder toute liste/arg multi-lignes avec virgule finale (le formateur la préserve ; piège connu si on écrit sur une ligne puis le formateur éclate sans virgule).
- L'ordre d'`addAll` dans `KittGame.onLoad` fixe la profondeur (CRT fond → scanner → voicebox → transcript → overlay).
